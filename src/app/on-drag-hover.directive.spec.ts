import { OnDragHoverDirective } from './on-drag-hover.directive';

describe('OnDragHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new OnDragHoverDirective();
    expect(directive).toBeTruthy();
  });
});
